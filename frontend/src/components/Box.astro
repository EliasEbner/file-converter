---
const { disableAnimation } = Astro.props;
---

<div class={`bg-white rounded-2xl shadow-lg p-4 lg:p-8 ${disableAnimation ? "" : "duration-1000 ease-out hover:scale-105 hover:shadow-2xl"}`}>
  <slot />
</div>

